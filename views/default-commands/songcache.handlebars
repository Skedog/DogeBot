<div class="inner-content-wrapper">
	<section class="content">
		<h1><i class="fa fa-magic"></i> !songcache</h1>
		<p>Returns a link to the song cache for the channel, and doesn't allow the same songs to be repeated within <code>x</code> time frame.</p>
		<h3>Permissions</h3>
		<p>The default permission level for <code>!songcache</code> is set to <code>everyone</code>.</p>
		<h3>Example</h3>
		<blockquote>
			<p><strong>User: </strong>!songcache</p>
			<p><strong>DogeBot: </strong>@user -> The song cache is available at: https://thedogebot.com/songcache/channel</p>
			<p><small>You can use the shorthand <code>!cache</code></small></p>
		</blockquote>
		<p><a href="/default-commands">&larr; Back to Default Commands</a></p>
	</section>
</div>