<div class="inner-content-wrapper">
	<section class="content">
		<h1><i class="fa fa-magic"></i> !promote</h1>
		<p>Promotes <code>queue position</code> to be the next song played.</p>
		<h3>Permissions</h3>
		<p>The default permission level for <code>!promote</code> is set to <code>mods and above</code>.</p>
		<h3>Example</h3>
		<blockquote>
			<p><strong>Modded User: </strong>!promote 6</p>
			<p><strong>DogeBot: </strong>@user -> Song #6 has been promoted!</p>
		</blockquote>
		<p><a href="/default-commands">&larr; Back to Default Commands</a></p>
	</section>
</div>