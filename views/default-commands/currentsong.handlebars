<div class="inner-content-wrapper">
	<section class="content">
		<h1><i class="fa fa-magic"></i> !currentsong</h1>
		<p>Returns the currently playing song.</p>
		<h3>Permissions</h3>
		<p>The default permission level for <code>!currentsong</code> is set to <code>everyone</code>.</p>
		<h3>Example</h3>
		<blockquote>
			<p><strong>User: </strong>!currentsong</p>
			<p><strong>DogeBot: </strong>@user -> The current song is "Test song" and it was requested by User</p>
			<p><small>You can use the shorthand <code>!cs</code> or <code>!song</code></small></p>
		</blockquote>
		<p><a href="/default-commands">&larr; Back to Default Commands</a></p>
	</section>
</div>