<h1><i class="fa fa-music"></i> Songlist  <small>{{{userData.urlChannel}}}</small></h1>
<p>Below is a list of your channel's song queue. Be sure to check out all the <a href="https://docs.thedogebot.com/default-commands" target="_blank">default commands</a> to understand what all you can do, like requesting a playlist from YouTube!</p>
<table class="datatable songs">
	<thead>
		<th>#</th>
		<th class="datatable-half">Title</th>
		<th>Video ID</th>
		<th>Requested</th>
	</thead>
	<tbody>{{{formattedSonglist}}}</tbody>
</table>
<div class="nosongs"></div>
<script>
	$(document).ready(function() {
		if (readCookie(page)) {
			dataTableStartSize = readCookie(page);
		} else {
			dataTableStartSize = '25';
		}
		buildDataTable('.datatable', dataTableStartSize);
	});
</script>