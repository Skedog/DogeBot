<h1><i class="fa fa-music"></i>  Songcache <small>{{{userData.urlChannel}}}</small></h1>
<p>Below is a list of your channel's song cache. This is a comprehensive list of every song ever requested in your channel!</p>
<div class="nosongs"><div class="gear"><div class="center"></div><div class="tooth"></div><div class="tooth"></div><div class="tooth"></div><div class="tooth"></div></div></div>
<table class="datatable songcache overflow-wrapper">
	<thead>
		<th>#</th>
		<th class="datatable-half">Title</th>
		<th>Video ID</th>
	</thead>
	<tbody>{{{formattedSongCache}}}</tbody>
</table>
<script>
	$(document).ready(function() {
		if (readCookie(page)) {
			dataTableStartSize = readCookie(page);
		} else {
			dataTableStartSize = '25';
		}
		setTimeout(function() {
			buildDataTable('.datatable', dataTableStartSize);
		}, 100);
	});
</script>